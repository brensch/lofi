[package]
name = "lofi-sim"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
lofi-core = { path = "../lofi-core", features = ["std"] }
lofi-app = { path = "../lofi-app", features = ["std"] }
