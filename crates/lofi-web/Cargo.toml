[package]
name = "lofi-web"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
lofi-app = { path = "../lofi-app" }
lofi-core = { path = "../lofi-core" }
